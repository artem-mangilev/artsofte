import { CardHiderPipe } from './card-hider.pipe';

describe('CardHiderPipe', () => {
  it('create an instance', () => {
    const pipe = new CardHiderPipe();
    expect(pipe).toBeTruthy();
  });
});
